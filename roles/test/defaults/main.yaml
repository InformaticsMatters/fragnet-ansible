---

# The fragnet search URL
# Define for the server you're actually testing.
fragnet_search_domain: "{{ lookup('env', 'FRAGNET_HOSTNAME') }}"
# A valid username and password
fragnet_username: "{{ lookup('env', 'FRAGNET_USERNAME') }}"
fragnet_password: "{{ lookup('env', 'FRAGNET_PASSWORD') }}"

# Fragnet API version
fragnet_api: v2

# The ID (used to select graph-specific tests).
fragnet_deployed_graph_id: toxcast_2019_10_24

# Keycloak credentials.
# Normally provided by credentials resident in AWX.

keycloak_server_url: ''
keycloak_realm: ''
